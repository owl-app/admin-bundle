<div id="equipment-details-attributes">
    <h4 class="ui top attached large header">{{ 'owl.ui.attributes'|trans }}</h4>
    <div class="ui attached segment">
        {% if equipment.attributes is not empty %}
            {% include '@OwlAdmin/Equipment/Attribute/_list.html.twig'  with {'attributes' : equipment.attributes} %}
        {% else %}
            {{ 'owl.ui.no_attributes'|trans }}
        {% endif %}
    </div>
</div>
